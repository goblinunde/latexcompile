# LaTeX Compiler Pro - 单文件版使用说明

## 🚀 快速开始

### 单文件版 (推荐 - 便于迁移)

```bash
# 复制到任何位置
cp latexcompile-standalone.sh ~/bin/texc
chmod +x ~/bin/texc

# 直接使用
./latexcompile-standalone.sh
```

**优点**：

- ✅ 单个文件，无依赖
- ✅ 方便迁移和分享
- ✅ 1193行，包含所有功能
- ✅ 纯ASCII图标，终端通用

### 模块化版 (原版)

```bash
# 需要三个文件
latexcompile.sh      # 主脚本
themes.sh            # 主题定义 
ui_components.sh     # UI组件
```

**优点**：

- ✅ 代码分离，易于维护
- ✅ 可单独更新主题

---

## 🎨 功能特性

### 主题系统 (7种)

1. **nord** (默认) - 北欧极光，冷色调
2. **dracula** - 紫粉暗黑主题
3. **sakura** - 樱花粉嫩主题
4. **matrix** - 绿色黑客风格
5. **gruvbox** - 复古暖色调
6. **monokai** - 经典暗黑主题
7. **default** - 原始配色

### 核心功能

- 📋 **编译历史** - 自动记录最近10次编译
- 🚀 **快速重编译** - 一键重编译上次文件
- 📊 **字数统计** - 集成texcount
- ⚙️ **配置管理** - INI格式配置文件
- 🎨 **主题切换** - 实时预览和保存

---

## ⚙️ 配置文件

### 位置

- **用户配置**: `~/.latexrc`
- **编译历史**: `~/.latex_history`
- **项目配置**: `.latexcfg` (当前目录)

### 配置示例

```ini
[General]
default_engine = xelatex
auto_cleanup = false
auto_open_pdf = true

[Theme]
active_theme = nord

[Features]
enable_history = true
max_history = 10
```

---

## 📝 使用示例

### 命令行模式

```bash
# 默认引擎 (xelatex)
./latexcompile-standalone.sh document.tex

# 指定引擎
./latexcompile-standalone.sh document.tex -e pdflatex
./latexcompile-standalone.sh document.tex -e lualatex
```

### 交互式模式

```bash
./latexcompile-standalone.sh
```

菜单选项：

```
1) [>>>] Quick Recompile      - 快速重编译
2) [F] Compile with config    - 项目配置编译
3) [+] Compile interactively  - 交互式编译
4) [=] Word Count             - 字数统计
5) [@] Compilation History    - 查看历史
6) [*] Create/Update Config   - 创建配置
7) [#] Settings & Themes      - 设置和主题
8) [~] Clean                  - 清理文件
9) [Q] Quit                   - 退出
```

---

## 🔧 图标说明

**纯ASCII图标** (终端通用，无字体依赖)：

| 图标 | 含义 | 图标 | 含义 |
|------|------|------|------|
| `[OK]` | 成功 | `[X]` | 失败 |
| `[i]` | 信息 | `[!]` | 警告 |
| `>>>` | 快速操作 | `[F]` | 文件 |
| `[+]` | 添加 | `[*]` | 设置 |
| `[#]` | 主题 | `[~]` | 清理 |
| `[@]` | 历史 | `[=]` | 统计 |
| `[Q]` | 退出 | `->` | 箭头 |

---

## 🐛 常见问题

### Q: Emoji显示乱码？  

**A**: 使用单文件版 `latexcompile-standalone.sh`，已移除所有Emoji，改用纯ASCII字符。

### Q: 如何迁移脚本？  

**A**: 单文件版只需复制一个文件即可：

```bash
scp latexcompile-standalone.sh user@server:~/
```

### Q: 主题不生效？  

**A**: 确保终端支持24位真彩色。检查：

```bash
echo $COLORTERM  # 应显示 truecolor
```

### Q: 无法编译？  

**A**: 检查依赖：

```bash
which latexmk  # 必须
which texcount # 可选，用于字数统计
```

---

## 📊 对比表

| 特性 | 单文件版 | 模块化版 |
|------|----------|----------|
| 文件数 | 1个 | 3个 |
| 代码行数 | ~1193行 | ~1400行 |
| 迁移便利性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 维护性 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 图标支持 | ASCII | ASCII |
| 功能完整性 | 100% | 100% |

---

## 🎯 推荐用法

### 日常使用：单文件版

```bash
# 创建别名
echo "alias texc='~/Downloads/sh/latexcompile-standalone.sh'" >> ~/.zshrc
source ~/.zshrc

# 使用
cd your-latex-project/
texc document.tex
```

### 服务器/共享环境

```bash
# 单文件便于分享
scp latexcompile-standalone.sh server:/usr/local/bin/texc
```

---

## 📚 版本信息

- **版本**: v6.0-standalone
- **单文件行数**: 1193行
- **包含模块**: 主题系统 + UI组件 + 所有功能
- **图标类型**: 纯ASCII，无Emoji

---

**享受简洁高效的LaTeX编译体验！** 🎉
